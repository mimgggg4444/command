

## 📁 파일 및 디렉토리 목록 출력 명령어

가장 기본적인 명령어는 `dir`입니다. 이는 현재 디렉토리와 그 하위 디렉토리의 파일 및 폴더 목록을 보여줍니다.

| 명령어 | 설명 | 예시 |
| :--- | :--- | :--- |
| `dir` | 현재 폴더의 파일 및 디렉토리 목록을 표시합니다. | `dir` |
| `dir /s` | 현재 폴더 및 **모든 하위 폴더**에 있는 파일 및 폴더 목록을 재귀적으로 표시합니다. | `dir /s` |
| `dir /b` | 머리글 정보나 요약 없이 파일 이름 및 디렉토리 이름만 표시합니다 (경로를 얻을 때 유용). | `dir /b` |
| `dir /s /b *.txt` | 현재 폴더 및 모든 하위 폴더에서 확장자가 `.txt`인 파일의 전체 경로를 표시합니다. | `dir /s /b *.txt` |

> **팁:** 출력이 너무 길어 한 화면에 다 보이지 않을 때는 `dir /p`를 사용하면 한 페이지씩 끊어서 볼 수 있습니다.

-----

## 📜 파일 내용 출력 명령어

특정 파일(주로 텍스트나 코드 파일)의 내용을 CMD 창에 직접 출력하려면 `type` 명령어를 사용합니다.

| 명령어 | 설명 | 예시 |
| :--- | :--- | :--- |
| `type [파일명]` | 지정된 **텍스트 파일의 내용**을 출력합니다. | `type index.html` |
| `type "C:\경로\파일.txt"` | 전체 경로를 사용하여 파일을 출력합니다. | `type "C:\Users\User\Documents\notes.txt"` |

### **파일 내용과 하부 디렉토리의 내용을 함께 다루는 방법**

CMD는 하나의 명령어로 "특정 폴더의 모든 파일 내용을 재귀적으로 출력"하는 기능을 기본으로 제공하지 않습니다. 하지만 `for` 루프와 `type` 명령어를 결합하여 원하는 결과를 얻을 수 있습니다.

**예시: 현재 폴더 및 모든 하위 폴더의 `.txt` 파일 내용 출력**

```cmd
for /r %%f in (*.txt) do (
    echo.
    echo ** 파일: "%%f" **
    echo ----------------------------------------
    type "%%f"
    echo ----------------------------------------
)
```

**설명:**

  * `for /r`: 현재 디렉토리와 그 하위 디렉토리(재귀적, Recursive)를 모두 검색합니다.
  * `%%f in (*.txt)`: 검색된 `.txt` 파일마다 루프를 실행하며, 파일 경로는 `%%f` 변수에 저장됩니다.
  * `echo.`: 빈 줄을 출력합니다.
  * `echo ** 파일: "%%f" **`: 현재 출력 중인 파일의 이름을 표시합니다.
  * `type "%%f"`: 해당 파일의 내용을 출력합니다.




---
---
---




``` cmd@echo off

@echo off

REM ** CMD 인코딩을 UTF-8(65001)로 임시 변경 **
chcp 65001 > nul 

setlocal enabledelayedexpansion

REM --- 1. 최종 출력 파일 이름 및 확장자 정의 ---
set "OUTPUT_FILE=project_scan_result.txt"
set "EXTENSIONS=*.js;*.jsx;*.ts;*.tsx;*.json;*.env;*.yml;*.yaml;*.md;*Dockerfile;*.gitignore;*.html;*.css;*.scss;*.less"

REM --- 기존 파일 삭제 (실행할 때마다 덮어쓰기 위해) ---
del "!OUTPUT_FILE!" 2>nul

REM --- 파일 시작 헤더 정보 (파일에만 저장) ---
echo ========================================================== >> "!OUTPUT_FILE!"
echo 🔎 프로젝트 파일 내용을 재귀적으로 탐색 및 출력합니다. >> "!OUTPUT_FILE!"
echo (탐색 시작 시간: %date% %time%) >> "!OUTPUT_FILE!"
echo ========================================================== >> "!OUTPUT_FILE!"
echo. >> "!OUTPUT_FILE!"

REM --- 2. 재귀적 탐색 시작 ---
for /r %%f in (%EXTENSIONS%) do (
    
    REM --- 3. node_modules 경로 포함 여부 확인 ---
    echo "%%f" | findstr /i /c:"node_modules" >nul
    
    if errorlevel 1 (
        REM --- 파일 경로 구분자 (파일에만 저장) ---
        echo. >> "!OUTPUT_FILE!"
        echo ---------------------------------------------------------- >> "!OUTPUT_FILE!"
        echo 📜 [ 파일 ] : "%%f" >> "!OUTPUT_FILE!"
        echo ---------------------------------------------------------- >> "!OUTPUT_FILE!"
        
        REM --- 파일 내용 복사 (파일에만 저장) ---
        type "%%f" >> "!OUTPUT_FILE!"
    )
)

REM --- 작업 완료 알림 (파일에만 저장) ---
echo. >> "!OUTPUT_FILE!"
echo ========================================================== >> "!OUTPUT_FILE!"
echo 탐색 완료. >> "!OUTPUT_FILE!"
echo ========================================================== >> "!OUTPUT_FILE!"


REM --- 콘솔 창에 최종 결과만 간단하게 출력 ---
echo 파일 탐색이 완료되었으며, 결과는 현재 폴더의 ["!OUTPUT_FILE!"]에 저장되었습니다.


REM ** 원래의 인코딩 (CP949)으로 복원 **
chcp 949 > nul 
endlocal

```

``` cmd
scan_project.bat > a.txt
```


```
# UTF-8 인코딩으로 저장
Get-ChildItem -Recurse -Include *.js,*.ts,*.jsx,*.tsx,*.json | Where-Object {$_.FullName -notmatch 'node_modules|\.git|dist|build'} | ForEach-Object {
    "========== $($_.Name) ==========" 
    Get-Content $_.FullName -Encoding UTF8
    ""
    ""
} | Out-File a.txt -Encoding UTF8

```
